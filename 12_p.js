// Q.12 how to split a sentence into array?

//   we will use split method to split a string into an array

let str = "Welcome to ARC tutorials";

const strArr = str.split(" ");

//  if we want to split it in the reverse form then do this
// const strArr = str.split(" ").reverse();
console.log(strArr);
